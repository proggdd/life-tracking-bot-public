# clasp deployment settings — contain your personal scriptId.
.clasp.json

# Local environment / secrets.
.env
.env.*
!.env.example

# Node / tooling caches (only relevant if you add local linters or tests).
node_modules/
npm-debug.log
package-lock.json

# Editor / OS cruft.
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Build artefacts — nothing here yet, but kept for safety.
dist/
build/
